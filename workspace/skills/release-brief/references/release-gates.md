# Release gates

Before launch, confirm that acceptance criteria are executable, all P0 risks are closed, every P1 has a handling plan, rollout scope is confirmed, rollback is executable, and owners are named.