# 待开放功能

以下页面在前端代码中已经存在，但当前没有出现在侧栏菜单中。它们可能仍处于联调、
灰度或待产品确认阶段，不应作为正式用户入口对外承诺。

| 页面 | 已实现的界面能力 |
|---|---|
| 知识库管理 | 分组、上传、解析状态、预览、替换、重建索引、限定文档问答 |
| 知识图谱 | 图谱空间、实体与关系浏览、版本查看、缩放与检索 |
| 运行观测 | 运行列表、状态、步骤、工具调用和结果 |
| 平台状态 | 基础设施、记忆维护预检、沙箱运行记录、业务域注册 |

代码中仍保留对应路由键和组件，但 `App.vue` 的当前菜单只挂载了正式开放页面。需要
开放时，应同时完成：

1. 产品与权限确认；
2. 在侧栏加入入口；
3. 验证直接 URL、刷新和浏览器前进后退；
4. 补齐权限、空状态和异常状态测试；
5. 把本页内容移入正式功能指南。

