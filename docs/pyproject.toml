[project]
name = "agent-platform-docs"
version = "0.1.0"
description = "AI Agent Platform user documentation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = []
include-package-data = true

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
dev = [
    "jupyter-book>=1.0.4.post1,<2.0.0",
    "furo>=2024.8.6",
]
